<UserControl
    x:Class="BTFX.Views.Measurement.Step3ReviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BTFX.ViewModels.Measurement"
    d:DataContext="{d:DesignInstance Type=vm:MeasurementViewModel}"
    d:DesignHeight="650"
    d:DesignWidth="750"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="ReviewCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FAFAFA" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="20,18" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>

        <Style x:Key="ReviewTitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#2F2A4A" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="ReviewControlTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="ReviewIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#33888888" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#44888888" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReviewProgressSliderStyle" TargetType="Slider">
            <Setter Property="Height" Value="36" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid
                            Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" VerticalAlignment="Center">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Height="4" Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="#FFFFFF" CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Height="4" Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="#8A8A8A" CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="12" Height="12">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="#FFFFFF" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReviewSpeedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Width" Value="64" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="SnapsToDevicePixels" Value="True" />
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Foreground" Value="#FFFFFF" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border
                                        x:Name="ItemBorder"
                                        Padding="10,6"
                                        Background="Transparent"
                                        CornerRadius="4">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#777777" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#7C7C7C" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            SnapsToDevicePixels="True">
                            <ToggleButton
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                ClickMode="Press"
                                Cursor="Hand"
                                Focusable="False"
                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                OverridesDefaultStyle="True">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border
                                            x:Name="OuterBorder"
                                            Background="Transparent"
                                            CornerRadius="6">
                                            <Path
                                                Width="8"
                                                Height="5"
                                                Margin="0,0,4,0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Data="M0,0 L4,4 L8,0"
                                                Fill="Transparent"
                                                Stroke="#FFFFFF"
                                                StrokeThickness="1.2" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="OuterBorder" Property="Background" Value="#33777777" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter
                                Margin="6,0,16,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                IsHitTestVisible="False" />
                            <Popup
                                AllowsTransparency="True"
                                Focusable="False"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                Placement="Bottom"
                                PopupAnimation="Fade">
                                <Border
                                    Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    Margin="0,4,0,0"
                                    Padding="4"
                                    Background="#646464"
                                    BorderBrush="#777777"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReviewPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48" />
            <Setter Property="MinWidth" Value="132" />
            <Setter Property="Padding" Value="22,0" />
            <Setter Property="Background" Value="#403B5B" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReviewSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48" />
            <Setter Property="MinWidth" Value="112" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#403B5B" />
            <Setter Property="BorderBrush" Value="#403B5B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F7F6FB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ReviewCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="36" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Border
                        Width="3"
                        Height="16"
                        VerticalAlignment="Center"
                        Background="#F2306A"
                        CornerRadius="2" />
                    <TextBlock
                        Margin="8,0,0,0"
                        Style="{StaticResource ReviewTitleTextStyle}"
                        Text="{DynamicResource MA.Step3.Title}" />
                </StackPanel>

                <Border
                    Grid.Row="2"
                    Background="Black"
                    ClipToBounds="True"
                    CornerRadius="8">
                    <Grid>
                        <Grid Visibility="{Binding HasDualVideo, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <MediaElement
                                x:Name="DualPreviewMediaElement"
                                LoadedBehavior="Manual"
                                MediaEnded="PreviewMediaElement_OnMediaEnded"
                                MediaFailed="PreviewMediaElement_OnMediaFailed"
                                MediaOpened="PreviewMediaElement_OnMediaOpened"
                                ScrubbingEnabled="True"
                                Stretch="Uniform"
                                UnloadedBehavior="Manual" />
                            <Image x:Name="FrontDualImage" Visibility="Collapsed" />
                            <Image x:Name="SideDualImage" Visibility="Collapsed" />
                        </Grid>

                        <Grid Visibility="{Binding HasDualVideo, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <MediaElement
                                x:Name="SinglePreviewMediaElement"
                                LoadedBehavior="Manual"
                                MediaEnded="PreviewMediaElement_OnMediaEnded"
                                MediaFailed="PreviewMediaElement_OnMediaFailed"
                                MediaOpened="PreviewMediaElement_OnMediaOpened"
                                ScrubbingEnabled="True"
                                Stretch="Uniform"
                                UnloadedBehavior="Manual" />
                            <Image
                                x:Name="FrontSingleImage"
                                Stretch="Uniform"
                                Visibility="Collapsed" />
                            <Image
                                x:Name="SideSingleImage"
                                Stretch="Uniform"
                                Visibility="Collapsed" />
                        </Grid>

                        <TextBlock
                            x:Name="PlaybackStatusTextBlock"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource AppFont}"
                            FontSize="14"
                            Foreground="#EAEAEA" />
                    </Grid>
                </Border>

                <Border
                    Grid.Row="4"
                    Padding="12,0"
                    Background="#646464"
                    CornerRadius="8">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            x:Name="PlayPauseButton"
                            Grid.Column="0"
                            Click="PlayPauseButton_OnClick"
                            Style="{StaticResource ReviewIconButtonStyle}"
                            ToolTip="{DynamicResource MA.Step3.Controls.Play}">
                            <materialDesign:PackIcon
                                x:Name="PlayPauseIcon"
                                Width="27"
                                Height="27"
                                Foreground="#FFFFFF"
                                Kind="Play" />
                        </Button>

                        <TextBlock
                            x:Name="CurrentTimeTextBlock"
                            Grid.Column="1"
                            Width="46"
                            Margin="8,0,10,0"
                            Style="{StaticResource ReviewControlTextStyle}"
                            Text="00:00" />

                        <Slider
                            x:Name="ProgressSlider"
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            IsMoveToPointEnabled="True"
                            Maximum="0"
                            Minimum="0"
                            PreviewMouseDown="ProgressSlider_OnPreviewMouseDown"
                            PreviewMouseMove="ProgressSlider_OnPreviewMouseMove"
                            PreviewMouseUp="ProgressSlider_OnPreviewMouseUp"
                            Style="{StaticResource ReviewProgressSliderStyle}"
                            ValueChanged="ProgressSlider_OnValueChanged"
                            Value="0" />

                        <TextBlock
                            x:Name="DurationTimeTextBlock"
                            Grid.Column="3"
                            Width="46"
                            Margin="10,0,8,0"
                            Style="{StaticResource ReviewControlTextStyle}"
                            Text="00:00" />

                        <ComboBox
                            x:Name="SpeedComboBox"
                            Grid.Column="4"
                            SelectedIndex="1"
                            SelectionChanged="SpeedComboBox_OnSelectionChanged"
                            Style="{StaticResource ReviewSpeedComboBoxStyle}">
                            <ComboBoxItem Content="0.5x" Tag="0.5" />
                            <ComboBoxItem Content="1x" Tag="1" />
                            <ComboBoxItem Content="1.25x" Tag="1.25" />
                            <ComboBoxItem Content="1.5x" Tag="1.5" />
                            <ComboBoxItem Content="2x" Tag="2" />
                        </ComboBox>
                    </Grid>
                </Border>

            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Button
                Margin="18,0,0,0"
                HorizontalAlignment="Left"
                Command="{Binding PreviousStepCommand}"
                Content="{DynamicResource MA.Step3.BackToImport}"
                Style="{StaticResource ReviewSecondaryButtonStyle}" />

            <Button
                Margin="0,0,18,0"
                HorizontalAlignment="Right"
                Command="{Binding BeginAnalyzeCommand}"
                Content="{DynamicResource MA.Step3.GoAnalyze}"
                IsEnabled="{Binding HasAnyVideo}"
                Style="{StaticResource ReviewPrimaryButtonStyle}" />
        </Grid>
    </Grid>
</UserControl>
