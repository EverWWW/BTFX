<UserControl
    x:Class="BTFX.Views.Settings.UserManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:BTFX.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="760"
    d:DesignWidth="1380"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="ToolbarButtonTemplateStyle" TargetType="Button">
                <Setter Property="Width" Value="180" />
                <Setter Property="Height" Value="68" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16"
                                SnapsToDevicePixels="True">
                                <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    RecognizesAccessKey="True" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.76" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ResetPasswordButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="120" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#403B5B" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="ResetBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="18">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ResetBorder" Property="Background" Value="#F7F6FB" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ResetBorder" Property="Opacity" Value="0.8" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
                <Setter Property="FontSize" Value="22" />
                <Setter Property="Foreground" Value="#666666" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="RowTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="Foreground" Value="#555555" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            </Style>

            <Style x:Key="ActionIconButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="38" />
                <Setter Property="Height" Value="38" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="ActionBorder"
                                Width="38"
                                Height="38"
                                Background="Transparent"
                                CornerRadius="19">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ActionBorder" Property="Background" Value="#EEF3F8" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.35" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PageArrowButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="ArrowBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ArrowBorder" Property="Background" Value="#F0F0F0" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.35" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PageNumberButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="44" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="PageBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="PageBorder" Property="Background" Value="#F0F0F0" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="40,40,40,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Background="#403B5B"
                Command="{Binding AddUserCommand}"
                Foreground="White"
                Style="{StaticResource ToolbarButtonTemplateStyle}">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"
                        FontFamily="Segoe UI Symbol"
                        FontSize="24"
                        Foreground="White"
                        Text="+" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource AppFont}"
                        FontSize="22"
                        FontWeight="Medium"
                        Foreground="White"
                        Text="添加" />
                </StackPanel>
            </Button>

            <Button
                Margin="32,0,0,0"
                Background="#FFFFFF"
                BorderBrush="#403B5B"
                BorderThickness="1"
                Command="{Binding LoadUsersCommand}"
                Foreground="#403B5B"
                Style="{StaticResource ToolbarButtonTemplateStyle}">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="20"
                        Foreground="#403B5B"
                        Text="&#xE72C;" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource AppFont}"
                        FontSize="22"
                        FontWeight="Medium"
                        Foreground="#403B5B"
                        Text="刷新" />
                </StackPanel>
            </Button>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="*" />
                <RowDefinition Height="32" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Background="#FAFAFA"
                CornerRadius="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.9*" />
                        <ColumnDefinition Width="1.15*" />
                        <ColumnDefinition Width="1.05*" />
                        <ColumnDefinition Width="1.0*" />
                        <ColumnDefinition Width="1.15*" />
                        <ColumnDefinition Width="1.15*" />
                        <ColumnDefinition Width="1.65*" />
                        <ColumnDefinition Width="1.35*" />
                        <ColumnDefinition Width="0.7*" />
                        <ColumnDefinition Width="0.7*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="编号" />
                    <TextBlock
                        Grid.Column="1"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="账号" />
                    <TextBlock
                        Grid.Column="2"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="姓名" />
                    <TextBlock
                        Grid.Column="3"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="角色" />
                    <TextBlock
                        Grid.Column="4"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="科室" />
                    <TextBlock
                        Grid.Column="5"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="状态" />
                    <TextBlock
                        Grid.Column="6"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="最后登录日期" />
                    <TextBlock
                        Grid.Column="7"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="重置密码" />
                    <TextBlock
                        Grid.Column="8"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="编辑" />
                    <TextBlock
                        Grid.Column="9"
                        Style="{StaticResource HeaderTextStyle}"
                        Text="删除" />
                </Grid>
            </Border>

            <Grid
                x:Name="UserListViewport"
                Grid.Row="1"
                VerticalAlignment="Stretch">
                <ItemsControl VerticalAlignment="Top" ItemsSource="{Binding Users}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                x:Name="RowBorder"
                                Height="60"
                                Margin="0,8,0,0"
                                Background="#FAFAFA"
                                CornerRadius="8"
                                MouseLeftButtonUp="RowBorder_MouseLeftButtonUp"
                                SnapsToDevicePixels="True">
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.9*" />
                                        <ColumnDefinition Width="1.15*" />
                                        <ColumnDefinition Width="1.05*" />
                                        <ColumnDefinition Width="1.0*" />
                                        <ColumnDefinition Width="1.15*" />
                                        <ColumnDefinition Width="1.15*" />
                                        <ColumnDefinition Width="1.65*" />
                                        <ColumnDefinition Width="1.35*" />
                                        <ColumnDefinition Width="0.7*" />
                                        <ColumnDefinition Width="0.7*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock
                                        Grid.Column="0"
                                        Style="{StaticResource RowTextStyle}"
                                        Text="{Binding RowNumber}" />
                                    <TextBlock
                                        Grid.Column="1"
                                        Style="{StaticResource RowTextStyle}"
                                        Text="{Binding Username}" />
                                    <TextBlock
                                        Grid.Column="2"
                                        Style="{StaticResource RowTextStyle}"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        Grid.Column="3"
                                        Style="{StaticResource RowTextStyle}"
                                        Text="{Binding RoleDisplay}" />
                                    <TextBlock
                                        Grid.Column="4"
                                        Style="{StaticResource RowTextStyle}"
                                        Text="{Binding DepartmentName}" />

                                    <Border
                                        Grid.Column="5"
                                        Width="120"
                                        Height="36"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        CornerRadius="18">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E9F7E3" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding User.IsEnabled}" Value="False">
                                                        <Setter Property="Background" Value="#E1E1E1" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Orientation="Horizontal">
                                            <Image
                                                Width="18"
                                                Height="18"
                                                RenderOptions.BitmapScalingMode="HighQuality">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Source" Value="/Resources/Images/DataManagement/yiwancheng.png" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding User.IsEnabled}" Value="False">
                                                                <Setter Property="Source" Value="/Resources/Images/Settings/yijinyong.png" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                            <TextBlock
                                                Margin="8,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource AppFont}"
                                                FontSize="18"
                                                FontWeight="Medium"
                                                Text="{Binding StatusDisplay}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#44BE13" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding User.IsEnabled}" Value="False">
                                                                <Setter Property="Foreground" Value="#B1B1B1" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock
                                        Grid.Column="6"
                                        Style="{StaticResource RowTextStyle}"
                                        Text="{Binding LastLoginDisplay}" />

                                    <Button
                                        Grid.Column="7"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ResetPasswordButtonStyle}">
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                            <Image
                                                Width="18"
                                                Height="18"
                                                RenderOptions.BitmapScalingMode="HighQuality"
                                                Source="/Resources/Images/Settings/chongzhimima.png" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource AppFont}"
                                                FontSize="18"
                                                FontWeight="Medium"
                                                Foreground="#403B5B"
                                                Text="重置密码" />
                                        </StackPanel>
                                    </Button>

                                    <Button
                                        Grid.Column="8"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ActionIconButtonStyle}">
                                        <Image
                                            Width="28"
                                            Height="28"
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            Source="/Resources/Images/DataManagement/bianji.png" />
                                    </Button>

                                    <Button
                                        Grid.Column="9"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style BasedOn="{StaticResource ActionIconButtonStyle}" TargetType="Button">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                                                        <Setter Property="IsEnabled" Value="False" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <Image
                                            Width="28"
                                            Height="28"
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            Source="/Resources/Images/DataManagement/shanchu2.png" />
                                    </Button>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsChecked}" Value="True">
                                    <Setter TargetName="RowBorder" Property="Background" Value="#F0F4F9" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <StackPanel
                Grid.Row="3"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Margin="0,0,30,0"
                    Command="{Binding PreviousPageCommand}"
                    IsEnabled="{Binding CanGoPrevious}"
                    Style="{StaticResource PageArrowButtonStyle}">
                    <Image
                        Width="20"
                        Height="20"
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Source="/Resources/Images/PatientSelectionView/zuo.png" />
                </Button>

                <ItemsControl Margin="4,0" ItemsSource="{Binding PageNumbers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="65" Height="40">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                            FontFamily="{StaticResource AppFont}"
                                    FontSize="22"
                                    FontWeight="Medium"
                                    Foreground="#999999"
                                    Text="...">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEllipsis}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <Border
                                    Width="44"
                                    Height="40"
                                    HorizontalAlignment="Center"
                                    Background="#403B5B"
                                    CornerRadius="8">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsCurrent}" Value="True" />
                                                        <Condition Binding="{Binding IsEllipsis}" Value="False" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                                FontFamily="{StaticResource AppFont}"
                                        FontSize="22"
                                        FontWeight="Medium"
                                        Foreground="White"
                                        Text="{Binding DisplayText}" />
                                </Border>

                                <Button
                                    Width="44"
                                    Height="40"
                                    HorizontalAlignment="Center"
                                    Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding PageNumber}">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource PageNumberButtonStyle}" TargetType="Button">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsEllipsis}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <TextBlock
                                                FontFamily="{StaticResource AppFont}"
                                        FontSize="22"
                                        FontWeight="Medium"
                                        Foreground="#555555"
                                        Text="{Binding DisplayText}" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button
                    Margin="30,0,0,0"
                    Command="{Binding NextPageCommand}"
                    IsEnabled="{Binding CanGoNext}"
                    Style="{StaticResource PageArrowButtonStyle}">
                    <Image
                        Width="20"
                        Height="20"
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Source="/Resources/Images/PatientSelectionView/you.png" />
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
