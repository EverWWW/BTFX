<UserControl
    x:Class="BTFX.Views.PatientSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    Background="{DynamicResource MaterialDesign.Brush.Background}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  DataGridCell style to remove focus border  -->
        <!--<Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Selection}"/>
                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>-->

        <!--  DataGridRow style to remove focus border  -->
        <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Top Bar  -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Back Button and Title  -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button
                    Command="{Binding BackCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="{DynamicResource Back}">
                    <materialDesign:PackIcon
                        Width="30"
                        Height="30"
                        Kind="ArrowLeft" />
                </Button>
                <TextBlock
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="24"
                    FontWeight="Bold"
                    Text="{DynamicResource PatientSelectionTitle}" />
            </StackPanel>

            <!--  User Info  -->
            <TextBlock
                Grid.Column="2"
                VerticalAlignment="Center"
                FontSize="14"
                Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                Text="{Binding CurrentUserInfo}" />
        </Grid>

        <!--  Toolbar  -->
        <Grid Grid.Row="1" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Search Box  -->
            <TextBox
                Grid.Column="0"
                Width="400"
                HorizontalAlignment="Left"
                materialDesign:HintAssist.Hint="{DynamicResource SearchPatient}"
                MaxLength="10"
                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}" />
                </TextBox.InputBindings>
            </TextBox>

            <!--  Action Buttons  -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button
                    Margin="8,0"
                    Command="{Binding AddPatientCommand}"
                    Content="{DynamicResource AddPatient}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Visibility="{Binding CanAddPatient, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Margin="0,0,8,0"
                    Command="{Binding ImportCommand}"
                    Content="{DynamicResource Import}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Visibility="{Binding CanImportExport, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button
                    Command="{Binding ExportCommand}"
                    Content="{DynamicResource Export}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Visibility="{Binding CanImportExport, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Grid>

        <!--  Patient List  -->
        <DataGrid
            Grid.Row="2"
            Margin="0,16,0,0"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeRows="False"
            ColumnHeaderHeight="56"
            GridLinesVisibility="Horizontal"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding Patients}"
            RowHeight="48"
            SelectedItem="{Binding SelectedPatient}"
            SelectionMode="Single">

            <DataGrid.Resources>
                <!--  Cell vertical alignment  -->
                <Style BasedOn="{StaticResource {x:Type DataGridCell}}" TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border
                                    Padding="8,0"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="80"
                    Binding="{Binding Id}"
                    Header="{DynamicResource Id}" />
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding Name}"
                    Header="{DynamicResource PatientName}" />
                <DataGridTextColumn
                    Width="60"
                    Binding="{Binding GenderDisplay}"
                    Header="{DynamicResource Gender}" />
                <DataGridTextColumn
                    Width="60"
                    Binding="{Binding Age}"
                    Header="{DynamicResource Age}" />
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding Phone}"
                    Header="{DynamicResource Phone}" />
                <DataGridTextColumn
                    Width="180"
                    Binding="{Binding IdNumber}"
                    Header="{DynamicResource IdNumber}" />
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}"
                    Header="{DynamicResource CreatedAt}" />
                <DataGridTemplateColumn Width="160" Header="{DynamicResource Actions}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                <Button
                                    Padding="8,4"
                                    Command="{Binding DataContext.EditPatientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="{DynamicResource Edit}"
                                    Style="{StaticResource MaterialDesignFlatButton}" />
                                <Button
                                    Margin="8,0,0,0"
                                    Padding="8,4"
                                    Command="{Binding DataContext.DeletePatientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="{DynamicResource Delete}"
                                    Foreground="{DynamicResource MaterialDesign.Brush.ValidationError}"
                                    Style="{StaticResource MaterialDesignFlatButton}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding PatientDoubleClickCommand}" CommandParameter="{Binding SelectedPatient}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </DataGrid>

        <!--  Bottom Bar: Pagination and Actions  -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Pagination  -->
            <StackPanel
                Grid.Column="0"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Command="{Binding PreviousPageCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="{DynamicResource PreviousPage}">
                    <materialDesign:PackIcon Kind="ChevronLeft" />
                </Button>
                <TextBlock
                    Margin="8,0"
                    VerticalAlignment="Center"
                    FontSize="14">
                    <Run Text="{DynamicResource Page}" /><Run Text="{Binding CurrentPage, Mode=OneWay}" />
                </TextBlock>
                <TextBlock
                    Margin="4,0"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Text="/" />
                <TextBlock
                    Margin="8,0"
                    VerticalAlignment="Center"
                    FontSize="14">
                    <Run Text="{DynamicResource Of}" /><Run Text="{Binding TotalPages, Mode=OneWay}" />
                </TextBlock>
                <Button
                    Command="{Binding NextPageCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="{DynamicResource NextPage}">
                    <materialDesign:PackIcon Kind="ChevronRight" />
                </Button>
                <TextBlock
                    Margin="16,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="14"
                    Foreground="{DynamicResource MaterialDesign.Brush.Secondary}">
                    <Run Text="{DynamicResource Total}" /><Run Text="{Binding TotalRecords, Mode=OneWay}" /><Run Text="{DynamicResource Records}" />
                </TextBlock>
            </StackPanel>

            <!--  Confirm Button  -->
            <Button
                Grid.Column="1"
                Height="40"
                Padding="24,0"
                Command="{Binding ConfirmSelectCommand}"
                Content="{DynamicResource ConfirmSelect}"
                Style="{StaticResource MaterialDesignRaisedButton}" />
        </Grid>
    </Grid>
</UserControl>
