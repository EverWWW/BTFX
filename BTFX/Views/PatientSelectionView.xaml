<UserControl x:Class="BTFX.Views.PatientSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="{DynamicResource MaterialDesign.Brush.Background}">

    <UserControl.Resources>
        <!-- DataGridCell style to remove focus border -->
        <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Selection}"/>
                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGridRow style to remove focus border -->
        <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Back Button and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding BackCommand}"
                        ToolTip="{DynamicResource Back}">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24"/>
                </Button>
                <TextBlock Text="{DynamicResource PatientSelectionTitle}"
                           FontSize="24"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"/>
            </StackPanel>

            <!-- User Info -->
            <TextBlock Grid.Column="2"
                       Text="{Binding CurrentUserInfo}"
                       FontSize="14"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"/>
        </Grid>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <TextBox Grid.Column="0"
                     materialDesign:HintAssist.Hint="{DynamicResource SearchPatient}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="10"
                     Width="400"
                     HorizontalAlignment="Left">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding AddPatientCommand}"
                        Content="{DynamicResource AddPatient}"
                        Margin="8,0"
                        Visibility="{Binding CanAddPatient, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding ImportCommand}"
                        Content="{DynamicResource Import}"
                        Margin="0,0,8,0"
                        Visibility="{Binding CanImportExport, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding ExportCommand}"
                        Content="{DynamicResource Export}"
                        Visibility="{Binding CanImportExport, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>

        <!-- Patient List -->
        <DataGrid Grid.Row="2"
                  Margin="0,16,0,0"
                  ItemsSource="{Binding Patients}"
                  SelectedItem="{Binding SelectedPatient}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="48"
                  ColumnHeaderHeight="56">

            <DataGrid.Resources>
                <!--  Cell vertical alignment  -->
                <Style BasedOn="{StaticResource {x:Type DataGridCell}}" TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border
                                    Padding="8,0"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource Id}" 
                                    Binding="{Binding Id}" 
                                    Width="80"/>
                <DataGridTextColumn Header="{DynamicResource PatientName}" 
                                    Binding="{Binding Name}" 
                                    Width="120"/>
                <DataGridTextColumn Header="{DynamicResource Gender}" 
                                    Binding="{Binding GenderDisplay}" 
                                    Width="60"/>
                <DataGridTextColumn Header="{DynamicResource Age}" 
                                    Binding="{Binding Age}" 
                                    Width="60"/>
                <DataGridTextColumn Header="{DynamicResource Phone}" 
                                    Binding="{Binding Phone}" 
                                    Width="120"/>
                <DataGridTextColumn Header="{DynamicResource IdNumber}" 
                                    Binding="{Binding IdNumber}" 
                                    Width="180"/>
                <DataGridTextColumn Header="{DynamicResource CreatedAt}" 
                                    Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" 
                                    Width="150"/>
                <DataGridTemplateColumn Header="{DynamicResource Actions}" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Command="{Binding DataContext.EditPatientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Content="{DynamicResource Edit}"
                                        Padding="8,4"/>
                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Command="{Binding DataContext.DeletePatientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Content="{DynamicResource Delete}"
                                        Foreground="{DynamicResource MaterialDesign.Brush.ValidationError}"
                                        Padding="8,4"
                                        Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding PatientDoubleClickCommand}" 
                                           CommandParameter="{Binding SelectedPatient}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </DataGrid>

        <!-- Bottom Bar: Pagination and Actions -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Pagination -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding PreviousPageCommand}"
                        ToolTip="{DynamicResource PreviousPage}">
                    <materialDesign:PackIcon Kind="ChevronLeft"/>
                </Button>
                <TextBlock FontSize="14"
                           VerticalAlignment="Center"
                           Margin="8,0">
                    <Run Text="Page "/><Run Text="{Binding CurrentPage, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Text="/"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="4,0"/>
                <TextBlock FontSize="14"
                           VerticalAlignment="Center"
                           Margin="8,0">
                    <Run Text="of "/><Run Text="{Binding TotalPages, Mode=OneWay}"/>
                </TextBlock>
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding NextPageCommand}"
                        ToolTip="{DynamicResource NextPage}">
                    <materialDesign:PackIcon Kind="ChevronRight"/>
                </Button>
                <TextBlock FontSize="14"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"
                           Foreground="{DynamicResource MaterialDesign.Brush.Secondary}">
                    <Run Text="Total: "/><Run Text="{Binding TotalRecords, Mode=OneWay}"/><Run Text=" records"/>
                </TextBlock>
            </StackPanel>

            <!-- Confirm Button -->
            <Button Grid.Column="1"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding ConfirmSelectCommand}"
                    Content="{DynamicResource ConfirmSelect}"
                    Height="40"
                    Padding="24,0"/>
        </Grid>
    </Grid>
</UserControl>
