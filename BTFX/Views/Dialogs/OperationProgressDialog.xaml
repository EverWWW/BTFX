<UserControl
    x:Class="BTFX.Views.Dialogs.OperationProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:BTFX.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="560"
    Height="410"
    FontFamily="{StaticResource AppFont}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="ProgressDialogCancelButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="160" />
            <Setter Property="Height" Value="52" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#403B5B" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#403B5B" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OperationProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="16" />
            <Setter Property="Foreground" Value="#403B5B" />
            <Setter Property="Background" Value="#EEF2F7" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border
                            Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                            <Grid x:Name="PART_Track" ClipToBounds="True">
                                <Border
                                    x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="8" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#FFFFFF" CornerRadius="8" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="76" />
                <RowDefinition Height="*" />
                <RowDefinition Height="84" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F5FAFF" CornerRadius="8,8,0,0">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource AppFont}"
                    FontSize="26"
                    FontWeight="Medium"
                    Foreground="#2F2A4A"
                    Text="{Binding Title}" />
            </Border>

            <Grid Grid.Row="1" Margin="44,28,44,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="72" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="76" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <materialDesign:PackIcon
                        Width="24"
                        Height="24"
                        Margin="0,2,10,0"
                        Foreground="#403B5B"
                        Kind="ProgressClock" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource AppFont}"
                        FontSize="22"
                        FontWeight="Medium"
                        Foreground="#333333"
                        Text="{Binding Stage}" />
                </StackPanel>

                <TextBlock
                    Grid.Row="1"
                    Margin="34,12,0,12"
                    VerticalAlignment="Top"
                    FontFamily="{StaticResource AppFont}"
                    FontSize="18"
                    Foreground="#666666"
                    LineHeight="28"
                    LineStackingStrategy="BlockLineHeight"
                    MaxHeight="58"
                    Text="{Binding Message}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="Wrap" />

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="64" />
                    </Grid.ColumnDefinitions>

                    <ProgressBar
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        IsIndeterminate="{Binding IsIndeterminate}"
                        Maximum="100"
                        Minimum="0"
                        Style="{StaticResource OperationProgressBarStyle}"
                        Value="{Binding Progress}" />

                    <TextBlock
                        Grid.Column="1"
                        Margin="14,0,0,0"
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource AppFont}"
                        FontSize="18"
                        Foreground="#403B5B"
                        Text="{Binding ProgressText}" />
                </Grid>

                <TextBlock
                    Grid.Row="3"
                    Margin="0,18,0,0"
                    VerticalAlignment="Top"
                    FontFamily="{StaticResource AppFont}"
                    FontSize="16"
                    Foreground="#999999"
                    LineHeight="30"
                    LineStackingStrategy="BlockLineHeight"
                    Padding="0,2,0,6"
                    Text="{DynamicResource OperationProgress.CancelHint}"
                    TextWrapping="Wrap"
                    Visibility="{Binding CanCancel, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>

            <Button
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Command="{Binding CancelCommand}"
                Content="{DynamicResource Cancel}"
                Style="{StaticResource ProgressDialogCancelButtonStyle}"
                Visibility="{Binding CanCancel, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </Grid>
    </Border>
</UserControl>
